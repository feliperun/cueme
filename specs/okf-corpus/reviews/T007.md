# Review — T007, diagnostics fora do modelo durável

**Veredito: aprovado**, rodada 0, com uma exceção de escopo concedida por mim e
duas intervenções estruturais. Commit `665595e`.

## O bloqueio — e por que o agente estava certo

O agente parou antes de terminar e reportou: deletar `MemoryNote.diagnostics`
quebra a compilação de `CueMeTests/SessionRecordTests.swift:65`, que assere
`decoded.diagnostics.events.isEmpty`. Esse arquivo está no escopo da **T014**, não
da T007.

Ele não tocou, não criou shim, não abriu exceção sozinho. Isso é exatamente o
comportamento que a `constitution.md` §1 pede, e o conflito é **defeito da minha
especificação**: a DoD da T007 exige que `MemoryNote.diagnostics` deixe de
existir, e a lista PODE TOCAR dela exclui justamente o arquivo que quebra em
consequência.

**Exceção concedida.** Deletei `testLegacyRecordWithoutDiagnosticsStillDecodes`.
Ele testa tolerância de decode para um campo que não existe mais — a T014 já
previa apagá-lo. Manter seria compatibilidade com formato morto, proibida pelo
ADR 0043.

## Verificação independente

| Comando | Resultado |
|---|---|
| `xcodebuild … -skip-testing:CueMeUITests test` | `** TEST SUCCEEDED **` |
| `sentrux check .` | Quality 7517, todas as regras passam |
| `sentrux gate .` | 7544 → 7517, sem degradação |

### Teste de mutação

Mutei `DiagnosticsLog.retentionDays` de 7 para 700.
`testRotationDropsLogsOlderThanSevenDays` **falhou**, os outros dez passaram. O
teste tem dentes e é o único que cobre aquela regra.

## Intervenções estruturais minhas

O commit foi bloqueado pelo hook com **dois** god files novos, nenhum tocado pela
tarefa: `SessionArchive.swift` e `SessionWorkspaceNavigation.swift`, ambos em 16.

Antes de dividir, verifiquei se o seam era honesto ou se eu estaria refatorando
por causa do medidor. Era honesto nos dois casos — cada arquivo hospedava **dois
tipos sem relação**:

- `SessionArchive.swift` → `SessionArchive` (renderização Markdown) +
  `SessionStore` (filesystem). É literalmente o split que o `design.md` §8
  antecipa, e a T013 deleta os dois.
- `SessionWorkspaceNavigation.swift` → `SessionWorkspaceTabs` (barra de abas) +
  `SessionWorkspacePane` (switch de painel). O segundo referencia 8 views de
  painel e é a fonte real do fan-out.

Os dois são moves puros.

## Nota sobre o Sentrux, acumulada em quatro tarefas

Quatro vezes agora, acrescentar arquivo em `CueMe/Model/OKF/` fez o `check`
acusar god file num arquivo que ninguém tocou. A causa é visível na saída:
`resolve: 1 resolved, 307 unresolved` — o Sentrux resolve quase nada por import e
constrói o grafo casando nomes de chamada contra símbolos de qualquer arquivo.
Nomes genéricos como `decode`, `encode`, `write`, `record` criam aresta espúria.

Não vou afrouxar a regra: até aqui, todo arquivo acusado tinha seam real e saiu
melhor. Mas é imposto recorrente, e os agentes foram instruídos a **parar e
reportar** em vez de refatorar fora do escopo deles — a decisão de dividir é do
orquestrador, para que ninguém divida um arquivo arbitrariamente só para o
medidor passar.

## Achados (nenhum bloqueante)

**1. Arquivos de teste novos fora da lista PODE TOCAR.** `NoteIntegrityTests.swift`
e `DiagnosticsLogTests.swift` são nomeados na tabela de Testes da própria tarefa,
mas a seção Escopo não os lista. Omissão minha; ele sinalizou em vez de assumir.

**2. AC2 cita `CorpusStore.rootURL`, que só existe na T013.** Ele testou contra um
diretório temporário injetado e ainda assertou que o default cai em
`~/Library/Logs/CueMe`. Solução correta para uma AC escrita antes da hora.

**3. `.sentrux/baseline.json` foi regravado por ele** (o `AGENTS.md` manda rodar
`gate --save` antes de editar arquivo existente). Descartei: o baseline deste
plano foi fixado por mim no início e é o que mantém o ratchet ao longo das 22
tarefas.

**4. `SessionPerformanceReport` deletado inteiro**, não só desligado das views.
Necessário para o AC6 fechar, e coerente com a política greenfield.

**5. Import de áudio agora incrementa `integrity.errors` diretamente.** Leitura
razoável do AC4 ("durante a sessão"), declarada. Aceito.

## AC × teste

| AC | Prova |
|---|---|
| AC1 — nota salva guarda só os contadores | `testSavedNoteKeepsOnlyIntegrityCounters` |
| AC2 — log fora do corpus | `testDiagnosticsLogWritesOutsideTheCorpus` |
| AC3 — painel mostra os 4 agregados | `testIntegrityReportShowsFourAggregates` |
| AC4 — recovery/error incrementam | `testRecoveryAndErrorEventsIncrementIntegrity` |
| AC5 — rotação de 7 dias | `testRotationDropsLogsOlderThanSevenDays` (+ mutação) |
| AC6 — `SessionDiagnostics` só no próprio arquivo e no log | grep, conferido |
